The storage permission saves the user's Streamshade preferences, onboarding completion, and aggregate counts of detected ad breaks and observed bonus claims in chrome.storage.local. This keeps preferences consistent across the popup, settings page, and Twitch tabs and preserves them between browser sessions. The extension does not persist chat messages, channel history, authorization headers, or playback tokens in extension storage. It does not use Chrome sync storage.
